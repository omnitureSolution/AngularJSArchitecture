'use strict';
define(['SignalR-Service'], function () {
    angular.module('XGENModule').service('CommonMessageService', ['$rootScope', 'SignalRService', '$location',
        function ($rootScope, SignalRService, $location) {
            var service = {
                
            };

            return service;

         
        }]);
});